class Relexais{
  final String name;
  final String sugar;
  final int strength;

  Relexais(this.name, this.sugar, this.strength);

  // Now we will make a function so that we could make a list of each collection
  // We will do this in Database File where we have Provider instance

}